{
  "name": "@ark/chronoperates-api",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "test": "PYTHONPATH=. uv run python -m pytest tests/ -v",
    "lint": "echo 'No lint yet'",
    "build": "echo 'Python project - no build step needed'"
  }
}